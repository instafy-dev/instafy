# Cloudflare configuration used for:
# - Cloudflare Pages deployments (frontend)
# - DNS automation (records / delegation)
#
# Note: self-hosted tunneling uses the tunnel broker (`TUNNEL_BROKER_*`), not Cloudflare Tunnel.

CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ZONE_ID=


# OpenAI API key for server-side local tooling. Copy this file to
# `.env.openai` (gitignored) when using the speech proxy helpers.
#
# For a per-user hosted connection, paste the key into Instafy's
# "OpenAI API key" connection flow instead. Never expose this as a VITE_ value.
OPENAI_API_KEY=

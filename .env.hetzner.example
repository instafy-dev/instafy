# Hetzner Cloud / Pulumi configuration.
# Copy to `$INSTAFY_ENV_DIR/.env.hetzner` (mode 0600) and fill in values.

# Preferred name used by infra scripts.
HETZNER_TOKEN=

# Legacy fallback used by some tooling/docs.
API_KEY=
